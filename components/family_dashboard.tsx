import { BookOpen, CalendarDays, Image, Music2, TrendingUp } from "lucide-react"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Badge } from "@/components/ui/badge"

export function FamilyDashboard() { return <section id="dashboard" className="flex flex-col gap-5"><div className="flex items-end justify-between"><div><p className="text-sm font-semibold uppercase tracking-wider text-secondary">Tu espacio familiar</p><h2 className="mt-1 text-2xl font-bold tracking-tight text-primary sm:text-3xl">Acompañar también es recordar</h2></div><Badge variant="outline" className="hidden sm:flex">Actualizado hoy</Badge></div><div className="grid gap-4 sm:grid-cols-3"><Metric icon={<CalendarDays />} label="Sesiones realizadas" value="12" detail="+3 este mes" /><Metric icon={<TrendingUp />} label="Última sesión" value="Receptivo" detail="Hoy, 10:30 a. m." /><Metric icon={<BookOpen />} label="Contenido subido" value="24" detail="12 fotos · 8 notas · 4 canciones" /></div><Card className="border-0 shadow-sm ring-1 ring-border/70"><CardHeader><div className="flex items-center justify-between"><div><CardTitle className="text-lg">Respuestas positivas por semana</CardTitle><p className="mt-1 text-sm text-muted-foreground">Una señal de cómo se sienten durante las actividades</p></div><TrendingUp className="text-secondary" /></div></CardHeader><CardContent><div className="flex h-36 items-end gap-3 border-b border-l px-4 pb-0 pt-4">{[42, 58, 48, 72, 64, 82, 92].map((height, i) => <div key={i} className="flex flex-1 flex-col items-center gap-2"><div className="w-full max-w-10 rounded-t-md bg-gradient-to-t from-primary to-primary/55" style={{ height: `${height}%` }} /><span className="text-xs text-muted-foreground">S{i + 1}</span></div>)}</div></CardContent></Card><div className="grid gap-4 md:grid-cols-2"><Card className="border-0 bg-accent/25 shadow-sm"><CardHeader><CardTitle className="text-lg">Áreas que necesitan más contenido</CardTitle></CardHeader><CardContent className="flex flex-col gap-3 text-sm"><p className="flex items-center gap-2"><Image className="size-4 text-primary" /> Fotos de viajes familiares <Badge className="ml-auto bg-primary">Sugerido</Badge></p><p className="flex items-center gap-2"><Music2 className="size-4 text-secondary" /> Música de su juventud <Badge variant="secondary" className="ml-auto">Pendiente</Badge></p></CardContent></Card><Card className="border-0 bg-primary text-primary-foreground shadow-sm"><CardContent className="flex h-full flex-col justify-center gap-2 p-6"><p className="font-semibold">Recuerda</p><p className="text-sm leading-6 text-primary-foreground/80">Esto no es un diagnóstico. Es un registro de actividades familiares para acompañar con cariño.</p></CardContent></Card></div></section> }
function Metric({ icon, label, value, detail }: { icon: React.ReactNode; label: string; value: string; detail: string }) { return <Card className="border-0 shadow-sm ring-1 ring-border/70"><CardContent className="p-5"><div className="mb-4 flex items-center justify-between"><div className="rounded-lg bg-primary/10 p-2 text-primary">{icon}</div><TrendingUp className="size-4 text-secondary" /></div><p className="text-sm text-muted-foreground">{label}</p><p className="mt-1 text-2xl font-bold text-primary">{value}</p><p className="mt-1 text-xs text-muted-foreground">{detail}</p></CardContent></Card> }

